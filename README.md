# redevops-connectors
ReDevOps Integration Plane — the IntegrationAdapter connector SDK: one contract, many governed provider adapters (OAuth/credentials via SecretRef), fixtures + conformance. Providers install on demand.
